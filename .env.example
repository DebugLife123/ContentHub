# ============================================================
# ContentHub 环境变量样例
#
# 复制成 .env 后填真实值（.env 已在 .gitignore 里，不会进仓库）：
#   cp .env.example .env
#
# 什么时候需要它：
#   - docker compose --profile full up   （容器部署，两个变量都是必需的）
#   - 直接用 java -jar 跑 docker profile
# 本地开发（application-dev.yml）有兜底值，不建 .env 也能跑 dev-up.ps1。
# ============================================================

# MySQL root 密码。
# 同时被 backend 当作 spring.datasource.password 使用，两边必须是同一个值。
# 注意：MYSQL_ROOT_PASSWORD 只在数据卷首次初始化时生效。
# 已经在跑的数据库改这个值不会改掉库里的密码，需要 ALTER USER 或重建数据卷。
MYSQL_ROOT_PASSWORD=change-me

# JWT 签名密钥，必须是 Base64 编码的 64 字节（代码走 HS512，长度不足会启动失败）。
# 生成方式（Linux / macOS / Windows PowerShell 均可）：
#   openssl rand -base64 64 | tr -d '\n'
# 或 PowerShell：
#   [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Max 256 }))
#
# 缺失时 docker compose 会直接报错退出——这是刻意的：
# 仓库里不再带任何可用的默认密钥，避免线上悄悄用了一个公开值。
JWT_SECRET=
